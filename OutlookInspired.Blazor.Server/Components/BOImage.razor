@using DevExpress.ExpressApp.Utils
<XafImage ImageName="@CaptionHelper.ApplicationModel.BOModel.GetClass(ObjectType).ImageName" Size="16" />

@code {
    [Parameter]
    public Type ObjectType { get; set; }
}